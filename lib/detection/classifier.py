# Simulação de classificador LLM (O "Juiz")
def classify_feedback(text):
    # Aqui entraria a chamada de API para gpt-4o-mini ou similar
    return {"is_feedback": True, "type": "correction", "severity": 3}
